export enum EChatStatus {
  Open,
  Closed,
}

export enum MatchingEnum {
  WORK,
  LEARN,
}

export enum ESocialLogin {
  Google,
  GitHub,
  LinkedIn,
}

export enum ESocialPlatform {
  Google,
  GitHub,
  Facebook,
  X,
  LinkedIn,
  Discord,
  Microsoft,
}
