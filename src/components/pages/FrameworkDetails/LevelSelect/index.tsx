'use client'

// Types
import { ILevel } from '@/types/pages/framework'

const LevelSelect = ({ levels }: { levels: ILevel[] }) => {
  return (
    <div>
      <p>select input</p>
    </div>
  )
}

export default LevelSelect
